#!/usr/bin/env php
<?php
$file = realpath( __DIR__.'/../autoload.php');
$file = str_replace( '\\', '/', $file );

require_once( $file );

use Monodb\Console;
$app = new Console();
$app->run();
exit(0);
